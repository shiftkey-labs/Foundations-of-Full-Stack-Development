# session 1 code examples

To install node modules, run:

```bash
npm install
```

To run the examples, use:

```bash
npm run dev
```

This will start a development server and you can view the examples in your browser.
